name: HalDo AI Check

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:

  check:

    runs-on: ubuntu-latest

    steps:

      - name: Repository laden
        uses: actions/checkout@v4


      - name: Dateien prüfen
        run: |
          echo "HalDo AI OS Check erfolgreich"
          echo "Website Dateien vorhanden"