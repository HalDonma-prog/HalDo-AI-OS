document.body.insertAdjacentHTML(
    "afterbegin",
    "<h2 style='color:#00aaff;text-align:center;'>💙 HalDo JS AKTIV</h2>"
);
/* =====================================
   HALDO AI OS 6.0 STABLE
   MAIN SYSTEM SCRIPT
===================================== */
console.log("💙 HalDo AI 6.0 Script aktiv");
alert("HalDo AI 6.0 Script läuft");

document.addEventListener(
    "DOMContentLoaded",
    () => {


        console.log(
            "💙 HalDo AI OS 6.0 wird gestartet..."
        );


        const bootScreen =
            document.getElementById(
                "boot-screen"
            );


        const app =
            document.getElementById(
                "app"
            );



        /*
            SYSTEM START
        */

        setTimeout(
            () => {


                if (bootScreen) {

                    bootScreen.style.display =
                    "none";

                }


                if (app) {

                    app.classList.remove(
                        "hidden"
                    );

                }


                console.log(
                    "🚀 HalDo AI OS 6.0 Stable ONLINE"
                );


            },
            2000
        );





        /*
            SYSTEM INFORMATION
        */


        const system = {

            name:
            "HalDo AI OS",

            version:
            "6.0 Stable",

            status:
            "Online",

            modules:
            [
                "AI Core",
                "Chat",
                "Notes",
                "PDF",
                "Mail"
            ]

        };


        window.HalDoSystem =
        system;



        console.log(
            system
        );





        /*
            UHRZEIT SYSTEM
        */


        function updateTime(){


            const now =
            new Date();


            console.log(
                "🕒",
                now.toLocaleString(
                    "de-DE"
                )
            );


        }



        updateTime();


        setInterval(
            updateTime,
            60000
        );



    }
);